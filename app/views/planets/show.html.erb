<div class="container col-5">
  <div class="card-trip">
    <%# <%= image_tag "#{@planet.name.downcase.gsub(" ", "_")}.jpg", alt: "no image" %>
    <div class="card-trip-infos">
      <div class="card-name-description">
        <h2><%=  @planet.name %></h2>
        <p><%= @planet.description %></p>
      </div>
      <div class="card-date-reservation">
        <h4> Available from <%= @planet.start_date.strftime('%d/%m/%y') %> to <%= @planet.end_date.strftime('%d/%m/%y') %></h4>
        <%= link_to 'Add a new reservation', new_planet_reservation_path(@planet), class: "btn btn-success" %>
      </div>
      <div class="card-trip-pricing">
        <h4>Price/night</h4>
        <h5><%= @planet.price %> €/guest</h5>
      </div>
    </div>
  </div>
  <%= link_to 'Edit planet', edit_planet_path(@planet) %> | <%= link_to 'Delete', planet_path(@planet), method: :delete, data: { confirm: 'Are you sure?' } %> | <%= link_to 'Back', planets_path %>
</div>
<div class="container">
  <div class="card-product">
    <h2> REVIEWS </h2>
    <% reservation = Reservation.find_by(planet_id: @planet.id) %>
    <% reviews = Review.find_by(reservation_id: reservation.id) %>

    <% reviews.each do |review| %>
    <%= review.note unless review_id.nil?%>
    <%= review.content unless review_id.nil?%>

    <% end %>

  </div>
</div>
